<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pb.mapper.ShiShiMapperCustom">
   <!-- <select id="queryUser" parameterType="com.pb.entity.User" resultType="java.lang.Integer">
        select count(id) from 管理人员表 where 用户名=#{user.用户名} and 密码=#{user.密码}
    </select>-->
</mapper>